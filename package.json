{
  "name": "check-code-coverage",
  "version": "2.0.0",
  "description": "This GitHub action reads code coverage result and fail if the actual code coverage is below a specified threshold.",
  "main": "index.js",
  "scripts": {
    "prepare": "ncc build index.js -o dist --license licenses.txt",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/themichaelhall/check-code-coverage.git"
  },
  "keywords": [
    "code",
    "coverage",
    "test",
    "github",
    "action"
  ],
  "author": "Michael Hall",
  "homepage": "https://github.com/themichaelhall/check-code-coverage",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0",
    "jest": "^29.2.0"
  }
}
